# Worklog — 2026-07-12

All shipped and confirmed live by Jun (deployed via Kamiak publish; no warnings).

## 1. Collapsible mobile control drawer (phone-test feedback on the mobile pass)
- Jun's phone test: sidebar stacks below the map and eats screen. Added a
  mobile-only sticky handle bar as first child of `#rail` (`#railHandle`:
  grab-bar + "hide/show controls" label; `display:none` on desktop).
- `#rail.collapsed` → max-height 34px, sections hidden; max-height transition;
  `map.resize()` ~280 ms after toggle so the MapLibre canvas refits;
  localStorage `railOpen` persists the choice.
- Phone can now be near-full-screen map: drawer handle + chip pill + legend
  chip, everything one tap away.

## 2. Cloudflare Web Analytics (task #34 ✓)
- pages.dev isn't a zone in Jun's account → dashboard auto-inject unavailable;
  Jun created the Web Analytics site manually ("Add a site" → hostname
  `nw-air-forecast.pages.dev`) and pasted the JS-snippet token.
- Beacon tag (token `0372013e…`; public by design — it can only *send*
  beacons, stats are private to the CF account) added to all four pages:
  viewer `<head>`, verify/about/data end of `<body>`. Publisher copies pages
  as-is; no script change needed.
- Dashboard: Cloudflare → Analytics & Logs → Web Analytics.

## 3. Data download page for researchers (task #36 ✓)
- New `pipeline/data.html` (light theme, matches about/verify): cycle dropdown
  from `cycles.json`; file cards rendered live from each cycle's
  `manifest.json` (names, sizes, grid, init time); verification
  summary/sites.json links; reader snippets (rasterio for COGs; numpy+gzip for
  bins); bin layout spec table; citation; bulk-download note.
- **Latest-only COGs** (Jun's choice): publisher `INCLUDE_COGS=1` copies
  `web_out/<latest>/*.tif` → `data/latest/` with a **25 MiB Pages per-file
  guard** (warn + skip). First deploy: no warning → COGs fit (~26 MB total for
  the pair, each under cap). Archived cycles = bins + manifest only; the page
  says so and drops the tif cards.
- Publisher stages `data.html` (no-cache) + `.tif` max-age=86400. Links:
  viewer sidebar "Data →", about.html nav.
- Tested with a node harness (mocked fetch/DOM): latest = 6 cards incl. tifs;
  archived cycle drops tifs, uses `data/<cycle>/`, shows the note.

## 4. About page people edit (Jun's wording)
- Priom Zarrah runs the CMAQ pipeline **with pipeline development support from
  graduate student Jahidul Islam** (Jahidul moved from the visualization
  sentence to the CMAQ sentence, now titled "graduate student").

## 5. Vertical scale on the 72-h popup chart (option A of 2 previews)
- `buildSpark`: left gutter X0 8→30; nice-step y-ticks (step from
  [1,2,5,10,20,25,50,100,200,500], ≤4 ticks) with faint gridlines + labels,
  0 at baseline. One change covers click popups, monitor popups, pin cards.
- Scrub mapping uses closure X0/X1 → still correct; show() clamps at hour 0
  over the gutter. Option B (labels at AQI band edges) previewed, not chosen.

## 6. Popup header slim-down + "latest obs 2 h behind chart" (07-14)
- Jun: big latest-AIRNOW block redundant vs the obs curve; also saw header
  stamp trail the curve's end by ~2 h.
- Diagnosis (live probes w/ EXACT prod params): no indexing bug — header used
  the page-load /api/obs snapshot (plus ≤10-min edge cache) while the curve
  fetches obs-series at click time; AirNow lag widens the gap. NOTE: the
  cowork web_fetch proxy served me a WEEK-old cached /api/obs first — always
  cache-bust (?fresh=…) when probing live endpoints.
- Fix: big block removed; buildSpark now renders a slim `AIRNOW latest: v ·
  time PT · category` line, seeded from /api/obs then overwritten from the
  SAME obs-series array the curve is drawn from (last non-null index) — header
  and curve can no longer disagree. siteClick keeps the full block only as
  fallback when there's no chart; "does not report" note unchanged.
- Also: obs-series' last two hourly values are often duplicates (AirNow posts
  a provisional current-hour value) — cosmetic, left as is.

## 7. Chart cursor dots differentiated (option B of 2 previews, 07-14)
- Was: model dot #2f6fd0 + obs dot #5b9bec — two blues, model dot didn't even
  match its white curve; cursor line also blue.
- Now: model = filled var(--ink) circle w/ var(--panel) outline; obs = hollow
  #5b9bec ring (reads even when stacked; echoes map's ring=obs language);
  cursor line neutral var(--ink-mute).

## 8. Popup time line redesign (option C of 3 previews, 07-14)
- Was `Tue Jul 14 14:00 · hr +13` → now `Tue Jul 14 · 2:00 PM PDT · forecast
  hour 13`; ptTimeInfo gained `time12` (12-h, auto PST/PDT — verified both).
- Obs-missing tail: future hours say `· no obs yet`, past station gaps say
  `· no obs` (future = DATA.epoch(h) > now).
- "AIRNOW latest" line switched to the same 12-h format.

## 9. Popup readout labels + in-chart legend + labeled AQI chip (07-14)
- Readout row → variant 2 of previews: `AIRPACT6: 18.4 µg/m³ · AIRNOW: 16.0
  µg/m³` (labels small, values .pop-v big; AIRNOW side all #5b9bec).
- Curve legend moved INSIDE the svg top-right (line swatches + names, y≈Y1+5;
  safe: ymax has 15% headroom above curves). Footer legend row removed
  (non-site latest-dot note kept as fallback).
- AQI chip labeled: `AIRPACT6 · Moderate · AQI 64` (Jun asked whether the chip
  was model or obs — it recomputes from the model curve at cursor hour).

## 10. Header order: AIRNOW latest above the skill card (07-14)
- buildSpark html now includes a hidden `spslot_K` div right under the latest
  line and returns `slotId`; siteClick passes it to attachSkill (falls back to
  the old VB div when there's no chart). Order: name → AIRNOW latest → skill
  card → readouts → time line → chart.
- Skill card margin `margin:6px 0 8px` for the new position.

## 11. Skill card window: day-1 × last 3 cycles (07-14)
- verify_airnow.py digest rewritten: pm/o3 pool ONLY lead-day-1 records (first
  24 forecast hours) from the last 3 verified cycles (≤72 pairs/site); m8
  keeps the 30-day all-lead pool (1 pair/day — 3 cycles would be n=3).
  sites.json gains `day1_cycles:3`; unit-tested the aggregation (ld2 excluded,
  m8 pools all days).
- Viewer: hourly card label `Day-1 skill here, last 3 forecasts:` (falls back
  to old label if day1_cycles absent, e.g. stale cached sites.json); m8 keeps
  `30-day skill here:`. SKILL_MIN pm/o3 120→48 (≥2 of 3 windows).
- DEPLOY NOTE: after publish, regenerate on Kamiak: `python
  pipeline/verify_airnow.py` (aqf env) — or the nightly run picks it up.

## 12. AQI chips under their readout columns (07-14, post-deploy round 2)
- Readout row → two column divs; model chip (spc) moved from footer to under
  the AIRPACT6 value (prefix dropped — column position conveys source); NEW
  AIRNOW chip (spco) under the AIRNOW value, computed from obs at cursor hour,
  hidden when no obs / non-monitor popups. Both scrub-synced in show().
- Footer now pin-only (spacer span keeps pin right-aligned). .pop-cat class
  already carries dark text color — no inline color needed.

## 13. SITE HISTORY PANEL BUILT (task #38, 07-14/15) — pending deploy
Design approved via 4 preview rounds (entry chip · 4-series w/ toggles ·
custom date range · past=each day's own day-1, future=current cycle dashed).
- **verify_airnow.py**: verify_day collects `ser` per site (day-1 hourly obs;
  model filled independently of obs gaps so the stitched line is continuous);
  stored in history/*.json ("ser" kept OUT of summary.json — bloat). New
  write_site_series() → verification/series/<sid>.json (sid sanitized
  [^A-Za-z0-9_.-]→_): full-history flattened hourly arrays, hour k = start-day
  PT midnight + k, missing days null. Span = summary window (90 d retention).
  Unit-tested (gap days null, all-null species dropped, filename sanitize).
- **publisher**: copies verification/series/*.json; _headers max-age=3600.
- **viewer**: `.hist-chip` "site history →" after site name in BOTH hourly +
  daily popups (closes popup, opens panel). #histWrap backdrop + .hist-panel
  (min(680px,100%-16px), Esc/×/backdrop close). Controls: PM2.5/Ozone ·
  7/14/30d presets (anchored to now, future included) · custom popover
  (from/to date inputs clamped to record + month quick chips + "records begin"
  note). 4 series: hourly thin 0.42 opacity, 24-h trailing avg thick (avg24:
  ≥18/24 valid = 75% rule, tested); model dashed beyond now; AQI bands;
  nice-step y-axis; day gridlines w/ adaptive label step; now line. Legend
  entries = ☑ toggles. Hover readout line (date · 12-h PT · 4 values). Window
  stats (bias/RMSE/r/n on paired past hours, ≥24 required). CSV (time_pt,
  time_utc, 4 value cols) of visible window. Today's obs merged in from
  fetchObsSeries after load. HIST_CACHE per site; failed fetch → "not
  available yet" + cache cleared for retry.
- DST note: ptMidnightMs probes -07:00/-08:00 (tested Jul+Jan); series index
  assumes 24-h days — the Nov DST day will be off by 1 h internally (cosmetic;
  documented, accepted).
- Tested: node --check, jsdom full-panel harness (render/toggles/species/
  custom-range/CSV/close), pipeline unit tests.
- **DEPLOY**: commit/push → Kamiak pull → `python pipeline/verify_airnow.py
  --backfill 20260627` (regenerates history WITH ser + series/ files;
  re-fetches AirNow — chunked ≤24 h, fine) → `bash publish_cloudflare.sh`.

## 14. Site history follow-ups (07-15, after Jun's live test)
- Confirmed live (chip appeared after hard refresh — cached index.html, the
  usual gotcha #4). Series files verified on CDN (432 h Wenatchee).
- Pin cards lost the chip (only popups had it) → chip added to .pin-hd next to
  the title (`.pin-hist` excluded from the drag pointerdown, like .pin-x).
- Midnight-null bug fixed in verify_day: obs for the history series now
  recorded BEFORE the model-pair checks (cycle starts 01:00 PT, so slot 0 had
  no hidx → both curves null at every midnight). Model still null at 00:00
  (real cycle gap) — obs no longer inherits it. NEEDS `--backfill 20260627`
  rerun on next deploy to regenerate series files.

## 15. Header lines matched pair (07-15, 3 preview rounds)
- Skill card slimmed to ONE LINE (Jun kept the name-row history chip; earlier
  variant that folded the chip into the line was rejected): `⌾ day-1 skill
  (past 3 days): bias +2.1 · RMSE 5.3 µg/m³ · details →` (verify link kept);
  SKILL_LBL + pairs + skillPhrase moved into title tooltip. Same code path
  serves daily/MDA8 (`30-day skill:`). Popup ~34 px shorter.
- AIRNOW latest line restyled to match: `● AIRNOW latest: **5.4** µg/m³ ·
  9:00 PM PDT · **Good**` (filled dot = live measurement, ring = statistic;
  bold blue values on gray). Both the /api/obs seed and the series-synced
  update now render HTML (was textContent).

## 16. History panel: explicit close button (07-15)
- "close" button added to the panel footer next to ↓ CSV (× / Esc / backdrop
  click all still work — the footer button is the discoverable one, esp. on
  phones).

## 17. Second header hairline (07-15, design round 2 — first proposal to MOVE
the existing rule was rejected; Jun wanted both)
- Structure now: name row · rule (the existing siteClick wrapper border) ·
  AIRNOW latest · skill line · NEW rule (`spdiv_K`) · readouts/chart.
- spdiv hidden until latest line or skill line is visible (revealed by the
  obs-series update and by attachSkill when elId starts with 'spslot') — no
  stacked rules on sparse popups. Pin cards inherit.

## 18. Popup quick-wins batch (07-15, previewed as 1/2/3/4)
1. **Peak callout**: amber ring + dashed drop-line at model-curve argmax;
   clickable footer line `▲ peak 33.8 µg/m³ · Wed 6:00 PM PDT` jumps cursor.
2. **CSV in popup footer**: 72 rows time_pt/time_utc/forecast/obs (obs col
   fills once series loads); filename `<sid|lat_lon>_<sp>_<cycle>.csv`.
3. **Nearest monitor on map clicks**: havKm + compass8 (both unit-tested) over
   monMarkers; line `◉ nearest monitor: NAME → (11.2 km SE)` under the title
   → opens that site's popup. Skipped when obs layer empty.
4. **Pollutant switch in monitor popups**: PM2.5|O3 mini-seg (hist-btn style)
   right-aligned on the name row; rebuilds popup only — map layer/matrix
   untouched. Mechanism: siteClick(s,spOv) recurses with global `species`
   temporarily swapped; buildSpark now captures `SPX=species` at build time so
   ALL scrub closures (chips, obs series, AIRNOW-latest) use the popup's own
   species (verified no stray refs).

## 19. "forecast time series" section label (07-15)
- Small uppercase sidebar-style label under the second hairline, titling the
  forecast tier. Label lives INSIDE the spdiv container (hairline = its
  border-top), so the existing reveal logic (series update / attachSkill)
  shows both together — sparse popups get neither.

## 20. Popup tier rearrange (07-15, fixes toggle-too-close-to-× complaint)
- Jun: O3 toggle on the name row sat next to MapLibre's ×, easy misclick.
  Option B chosen from 2 previews, plus his rearrangement.
- New tiers: name / [AIRNOW latest + "site history →" chip on one row] /
  hairline + "FORECAST TIME SERIES" label + PM2.5|O3 toggle (always visible on
  monitor popups) / day-1 skill line / readouts / chart.
- Implementation: chip + toggle moved INTO buildSpark (chip wired in wire()
  via openHistory({id:siteId,name:title,lon,lat}); toggle ids ps1/ps2_K, wired
  by siteClick via returned sp.K). siteClick title row back to plain name;
  old VB-based chip/toggle wiring removed. Pin cards: body has neither
  (noPin), keep the pin-hd chip. Skill slot now sits under the label row —
  reads as the forecast section's calibration note.

## 21. Pin cards brought up to popup parity (07-15, Jun caught the gap)
- Pin-card body now shows the full tier layout: AIRNOW latest + history chip
  row, label row WITH PM2.5|O3 toggle, and the day-1 skill line (attachSkill
  now called from createPinCard). Header chip removed (was duplicating).
- Toggle on a pinned card: closeCard → createPinCard(...,spOv) (same
  temporary-species-swap recursion as siteClick; obs arg dropped on switch —
  it belongs to the original species, the obs series refills the line).

## 22. Forecast tier declutter — option A + edge toggle (07-15)
- Jun: tier below FORECAST TIME SERIES got busy. Option A of 2 previews:
  readout collapsed to ONE row (`AIRPACT6: 18.4 [AQI 64]  AIRNOW: 16.0
  [AQI 59] µg/m³`); chips shrunk to `AQI n`, category name → title tooltip;
  shared unit label. (Option B — merged peak/time row + dropping the in-chart
  legend — previewed, not chosen.)
- PM2.5|O3 toggle nudged to ~8 px from the popup's right edge
  (margin-right:-5px vs the 13 px content padding).
- Saves two rows in popups AND pin cards.

## 23. Toggle alignment hardening (07-15, from Jun's screenshot)
- Screenshot showed the PM popup's toggle sitting right after the label
  despite inline `justify-content:space-between` (unreproducible headlessly —
  no browser in sandbox, Chrome ext disconnected; no CSS override found).
- Fix is layout-mode-proof: toggle span + history chip get `margin-left:auto`
  (wins under flex regardless of justify-content) AND `float:right` (block
  fallback; floats are ignored inside working flex, so no conflict). Chip now
  hugs right even when the latest line wraps it to its own row.

## 24. Two-tier staleness alert (07-18, after a real delayed-cycle morning)
- Context: cycle delayed; Jun saw no alert. Email chain was suspended (he'd
  scancel'ed the watcher) and STALE_HOURS=36 wasn't crossed — by design, but
  he wants same-day visibility.
- checkStaleness rewritten: tier 1 = after 08:00 PT (DELAY_ALERT_HOUR; publish
  is ~07:00 PT) with no cycle initialized today → amber dismissible pill
  ("Today's forecast (Sat Jul 18) is delayed — showing yesterday's cycle,
  still valid through <last hour's date>"); dismissal key delayDismiss_<PT
  date> so it returns next day. Tier 2 = old red >36 h bar unchanged, wins
  over amber, not dismissible. Only runs on the latest-cycle load path
  (archive browsing untouched). Decision matrix unit-tested (6 cases).

## 25. Drag-to-pin popups (option A of 2 previews, 07-18)
- Jun: users should be able to drag popups freely. Chose drag-to-pin over
  in-place popup dragging (setOffset + leader line): dragging a popup's title
  row >6 px converts it into the existing pin card mid-gesture.
- enableDragToPin(popup,...): grab handle = FIRST .pop-t (the title);
  pointerdown ignores a/.hist-chip/.hist-btn/.spark-pin targets; 6 px
  threshold distinguishes click from drag; on convert → popup.remove() +
  createPinCard(...,grab={x,y,pid}).
- createPinCard gained `grab`: positions card under the cursor, seeds the
  existing drag state, and tries setPointerCapture(grab.pid) to continue the
  gesture seamlessly (if a browser refuses mid-gesture capture transfer, the
  card simply drops there and the user re-grabs — graceful).
- Wired in siteClick (monitor popups) and queryPoint (map clicks); ⊕ pin
  button unchanged. Title rows get cursor:grab + tooltip.

## 26. VIIRS true-color satellite layer (07-18, Jun's idea)
- New Observations toggle "Satellite true color (daily)" → NASA GIBS
  VIIRS_SNPP_CorrectedReflectance_TrueColor XYZ tiles (epsg3857,
  GoogleMapsCompatible_Level9, maxzoom 9; no key, no Worker; tile URL
  verified live). Layer inserted ABOVE 'base', BELOW 'aq' → acts as a basemap
  swap; the model opacity slider is the reveal control.
- Date = PT day of the slider position (daily view: selected day), clamped to
  newest available (heuristic: today's image assumed from 15:00 PT — overpass
  ~13:30 + processing; else yesterday). tcSync() in refresh() re-points tiles
  (setTiles, with remove/re-add fallback) only when the day changes; works in
  the archive (GIBS has full history). #tcChip bottom-left states
  "imagery: VIIRS true color · date · NASA GIBS".
- Pairs with HMS polygons + fire dots: four smoke-evidence sources composable.

## 27. Date in the collapsed chip pill (07-18)
- #miniDate (muted) added between pollutant and time in #mapTagMini: hourly
  `▶ PM2.5 · Sat Jul 18 · 14:00 PDT +13` (date rolls over as the animation
  crosses midnight); daily view sets it empty (label already is the date).

## 28. Desktop sidebar fold (07-18)
- #railFold: 14 px handle strip between #rail and #map-wrap (◀); click →
  body.rail-folded: rail width/flex-basis 0, handle widens to 26 px rail with
  vertical "CONTROLS" label (▶ to reopen). Width transition + map.resize()
  after 230 ms; localStorage 'railOpenDesk'.
- Desktop-only: fold CSS wrapped in @media (min-width:761px), handle
  display:none ≤760 px — mobile drawer (railHandle/railOpen) untouched; a
  stale rail-folded class can't zero the mobile drawer.

## 29. True color: auto-drop model opacity (07-18, Jun: "can't see the smoke")
- Defaults problem: model raster at 80% buried the imagery. Toggling
  truecolor ON now auto-sets the opacity slider to 35% (only if it was >45,
  previous value remembered in TC_PREV_OP); toggling OFF restores it.
  tcSetOpacity drives the real slider (dispatchEvent input) so UI, legend and
  layer stay in sync; user can still fine-tune while it's on.

## 30. TRUE hourly day-1 scatter on verify.html (07-18, Jun's question)
- Jun noticed the scatter's point count << hour×site. Root cause explained:
  main scatter = SITE-DAY MEANS (history stores only sufficient stats; page
  fine print said so) + ≤1500 downsample. KPIs/lead-hour bars were always
  true hourly.
- New pipeline artifact `verification/hourly-day1.json` (written from the
  trailing 14 ser_days in write_site_series): flat o/f/si arrays per species,
  si = index into states list (OTH always included). Unit-tested (pair count,
  null skipping, state indexing). Publisher copies it.
- verify.html: new card "Hourly pairs — day 1" between the two existing ones;
  respects the region filter via si; hidden for MDA8 or lead-day 2/3 filter
  (day-1 only); ≤3000 dots drawn (r=1.7, op .22) with stats line computed on
  ALL pairs (n · bias · RMSE · r · "day-1 forecasts only"). Methods note
  updated to distinguish the two scatters.
- DEPLOY: rerun verify (nightly or manual) to generate hourly-day1.json,
  then publish.

## 31. Per-site temporal correlation on verify.html (07-18)
- Context (Jun's methods questions): pooled r conflates spatial + temporal
  skill; per-site temporal r existed only in the site-history panel.
- New siteAgg(F): per-site pooled sufficient stats → per-site temporal
  bias/RMSE/r (r=null under 48 pairs; 8 for MDA8 — 1 pair/day). Unit-tested
  (perfect-corr → r=1.000, bias exact, small-n → null).
- KPI card: "Correlation r (pooled)" + line `median site r 0.61 · 44 sites`
  (needs ≥3 sites). Site table: r column, coral when r<0.4, dash when null.
  All filters honored; table() refactored onto siteAgg. Client-side only —
  no pipeline/deploy dependency beyond the page.

## Queue (unchanged priorities)
- #39 site retention window (HELD OFF 07-15): site 208 MB @ ~19 cycles, grows
  ~10 MB/day; publisher copies the whole archive each publish. Plan approved
  in concept: SITE_KEEP_DAYS=90 in publish_cloudflare.sh; revisit ~when site
  nears 1 GB or publish feels slow. R2 archive = phase 2.
- #35 accessibility pass (next in the maturity list)
- #37 smoke-day indicator badge
- #38 site history panel (obs vs stitched day-1 forecasts, successor to
  airpact.wsu.edu/performance.php; previews approved 07-14; CSV export +
  24-h-mean toggle ideas noted there too, not yet queued)
- #25 Phase 2 GOES-R FDC FRP (Kamiak job)
- Monthly statistics on /verify.html (~Aug 1)
