# Worklog — 2026-07-07

## Shipped, deployed, and user-verified live
1. **Default view = PM2.5 concentration** (was AQI). `colorMode='conc'` + Concentration button starts active. Viewer line ~500/516.
2. **Real AirNow monitor observations** replacing synthetic points:
   - New `pipeline/functions/api/obs.js` — Cloudflare Pages Function at `/api/obs`. Queries AirNow `/aq/data` (bbox −125.92,39.79,−109.59,49.84, PM25+OZONE, `dataType=B`, last 3 h → newest per site+param), 10-min edge cache, key from Pages secret `AIRNOW_API_KEY` (set 2026-07-07 via `wrangler pages secret put`). Returns `{fetched, sites:[{name,agency,lon,lat,pm:{v,aqi,unit,utc},o3:{...}}]}`.
   - Viewer: dots colored by obs (follows AQI/conc color mode); popup "AIRNOW observed" + "AIRPACT6 model" comparison; timestamps rendered in Pacific time (auto PST/PDT); graceful grey-out "obs unavailable" when endpoint missing (local preview, embeds, no key).
   - `publish_cloudflare.sh` restructured: `$ROOT/functions` next to `$ROOT/public` (wrangler requirement), `cd $ROOT` before deploy.

## Committed + pushed, NOT yet deploy-tested  ← START HERE TOMORROW
3. **Past-forecast archive**: publisher stages every `web_out/<cycle>` under `data/<cycle>/` + writes `data/cycles.json`; viewer fetches it, injects "Forecast cycle" `<select>` (top of sidebar), `switchCycle()` reloads via refactored `loadCycleData(url)`, shows "Archived forecast · cycle X" badge. Retention: unlimited (Jun's choice). Wrangler content-hashing dedupes old uploads. Cloudflare limits checked: no storage concern (20k files free plan ≈ 13 yr at 4 files/cycle).
4. **Gzip compression**: publisher's `stage_cycle()` ships `.bin.gz` only (gzip cached next to source bin in web_out, compressed once); viewer `fetchU16()` tries `.gz` + pako.inflate, falls back to raw `.bin`; pako added to viewer `<head>`; `build_embed.py` guards against double pako injection. `_headers`: `:cycle/*.bin.gz` immutable, `latest/*.bin.gz` 86400, `cycles.json` no-cache.
5. **Staleness banner**: `checkStaleness(mf)` — if forecast start (`mf.hours[0].utc`) older than `STALE_HOURS=36`, orange warning banner top-center of map. Latest view only.
6. **72-h sparkline popups** (mockup approved by Jun): `buildSpark(lng,lat,obs)` — SVG line w/ AQI band shading (from PM_BP/O3_BP), midnight ticks, hover-to-inspect, cursor synced to animation via `sparkSync` hook in `refresh()`; NaN gaps split the line. Map click (hourly) uses it; monitor click adds obs as blue-ringed point at its actual time (needs `DATA.epoch(h)` — added to both data builders); daily view click gets 3 daily AQI bars (`dailyBarsHTML`). Popups `maxWidth:'372px'`.

## Verification done in-session
- `node --check` on all viewer scripts + obs.js after every change; `bash -n` publish script; py_compile build_embed
- Live `/api/obs` inspected: concentrations + AQI both real (e.g. Port Alberni pm v=5.4 UG/M3, aqi=30)
- Sparkline band/hover math unit-tested in node; cycles.json generation simulated

## Tomorrow's checklist
1. Mac: commit/push anything uncommitted (`git status`); Kamiak: `git pull`, `bash publish_cloudflare.sh`
2. First publish gzips all archived cycles once — slower, expected
3. Test on site: (a) cycle selector appears + archived cycle loads, (b) network tab shows `.bin.gz` fetches + note actual size vs ~25 MB, (c) sparkline popups: map click, monitor click w/ obs dot, daily bars, cursor follows play, (d) console clean
4. Staleness banner can't be tested naturally — temporarily set `STALE_HOURS=0` locally if desired
5. Then: build **forecast verification page** (mockup approved). Plan: nightly Kamiak script fetches previous day's AirNow obs (historical query, same key), pairs with archived forecast at sites per lead hour, appends to published `verification.json`; new `verify.html` computes bias/RMSE/r client-side, filters by species/window/state/lead. Can backfill from July 3 archives. Adds 1 AirNow call/day.

## Deferred ideas (from feature discussion)
Use-my-location + search, shareable permalinks (hash state), fire/smoke overlays (HMS, FIRMS), publish-failure email alert, hide obs layer on archived cycles (currently obs are always current-time even against old forecasts — known, accepted for now).

## Repo state at end of session
origin/main = local main on Mac; Kamiak may need `git reset --hard origin/main` if it diverges again (it's a pure consumer). Uncommitted at session end: CLAUDE.md + memory/ (this file), README/viewer/publisher sparkline-batch changes were being committed by Jun — verify with `git status` tomorrow.
