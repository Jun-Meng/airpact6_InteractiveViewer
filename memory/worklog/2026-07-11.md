# Worklog — 2026-07-11

Note: the day's earlier entries were appended into `2026-07-10.md` before the
date rollover (again) — full technical detail there. This is the day summary.

## Shipped today (all deployed + confirmed by Jun through commit a5dd0b1)
1. **Collapsible map chip**: ▴ folds the control card to a one-line live pill (mini play + pollutant + ticking time); localStorage; matches legend-chip gesture.
2. **Pinnable comparison cards** (commit 92cbd89): "⊕ pin" on sparkline popups → draggable screen-anchored cards + color-matched origin dots (6-hue cycle); SPARK_SYNCS Set keeps every open cursor animated together; cards auto-close on cycle switch.
3. **Site capability markers** (0778623 + bugfix 52bbb9c): colored circle / hollow ring / gray diamond (= no sensor for selected pollutant); capability = latest obs ∪ series data; hover tooltip "measures PM2.5 only"; pollutant-aware key row in chip footer. BUG LESSON (memorize): on MapLibre marker elements NEVER overwrite className (kills marker positioning classes) and NEVER use CSS transform (conflicts w/ inline translate) — classList + clip-path instead.
4. **Layer matrix picker** (a5dd0b1) — the big one, fixed Jun's "two places to control what you see" complaint. Sidebar "What to show" matrix (PM2.5/Ozone/Overall × Hourly/Daily, 5 products, units shown, dead cell dashed) replaces the Pollutant seg AND the chip's view toggle; selectLayer() sets view/species/dailyPol atomically; AQI/Conc moved to legend header (locks on Overall); old hash params still restore; daily column greys when cycle lacks daily bins. Chose over chip-dropdown option after side-by-side previews (visibility > compactness).

## Final interface model (stable — think hard before disturbing)
- Sidebar: Find a place · What to show (matrix) · Forecast cycle · Overlays (3 collapsible groups)
- Map chip (collapsible): layer name + time readout + play/slider/speed + ticks + site-marker key
- Legend card (collapsible): title + AQI/Conc + opacity + color scale
- Hover = instant value · click = 72-h popup (pinnable) · everything folds to live pills

## Queue (unchanged)
- Phase 2 GOES-R FDC FRP via Kamiak job (task #25) — also feeds wildfire emission inventory
- Monthly statistics on /verify.html (~Aug 1; history/*.json has everything)
- Held: pollutant-into-chip (superseded by matrix), HMS was completed 07-10
