# Worklog — 2026-07-09/10

Note: several sessions between 07-08 evening and today were appended into
`2026-07-07.md`/`2026-07-08.md` before I noticed the date rollover — full
technical detail for those items lives there. This file is the clean summary
of where things stand as of end of 2026-07-10.

## Shipped since 07-08 (all deployed + confirmed unless noted)
- **MDA8 ozone verification** on /verify.html (third metric; 70 ppb NAAQS quadrants, bias-by-lead-day, exceedance hit/false-alarm counts). Backfilled. Deploy hit AirNow >24 h limit AGAIN → chunked fetch (23 h + 7 h spillover) + retry.
- **Per-site verification badges** in monitor popups (sites.json 30-day digest; pm/o3 hourly + m8; hidden below min pairs).
- **Publish-failure email**: SLURM --mail-type=FAIL,TIMEOUT on watcher + publish jobs; watcher re-arms BEFORE exiting 1 on a missed day.
- **Class I areas + tribal lands** overlays (EPA Living Atlas + TIGERweb via /api/overlay proxy, hover names).
- **Model grid (~4 km)** overlay (client-generated warped mesh, minzoom 6.5) + clicked-cell highlight.
- **VIIRS fire detections** (/api/fires, 3 satellites, FIRMS_MAP_KEY secret set) + clustering (cluster ≈ one fire, ΣFRP-sized, click-to-zoom) + map maxZoom 9→11.5.
- **HMS smoke plumes** (NESDIS SAB ArcGIS service — the on-hold blocker dissolved; density-styled, 20-min cache, live-cycle-only).
- **Overlay panel redesign**: 3 collapsible groups (Observations open w/ "not forecast" hint; Reference; Map), count badges, localStorage memory.
- **Topographic basemap** toggle (Esri World Topo, keyless, persisted).
- **Major highways**: built (TIGERweb), debugged twice (payload → Worker CPU limit → streaming fix in overlay.js), then REMOVED at Jun's call (topo basemap shows highways natively). Streaming fix kept — it protects all overlay sources.
- LinkedIn launch post drafted + OG meta tags + og-preview.png (1200×627) + robots.txt/404.html; LinkedIn preview cards remained flaky → recommended manual image attach.
- Pacific-time clock everywhere + map clock chip; Daily tab redesign (pollutant selector, day slider+play, model-vs-obs daily popup w/ obs MDA8); popup close buttons; hollow-ring no-obs dots; PM-only-site popup fix.

## Hard-won engineering rules (bit us this period)
1. AirNow /aq/data: ≤24 h per domain-wide query. ALWAYS.
2. Cloudflare Workers (free): never JSON.parse multi-MB bodies — stream via body.tee() + first-bytes peek (overlay.js pattern).
3. CF edge cache survives deploys — design payload consumers to tolerate stale schema (id + coordinate fallback matching).
4. Test live endpoints with the EXACT params production sends.
5. Viewer popups are dark-themed — no hardcoded dark strokes.

## Current work queue
- **Phase 2 fires** (task #25): GOES-R ABI FDC FRP via Kamiak job from NOAA AWS (noaa-goes18/19, ABI-L2-FDCC) — 5-min cadence; reusable for wildfire emission inventory.
- **Monthly statistics** (~Aug 1, needs full forecast month; archives start 06-27): monthly verification roll-ups on /verify.html; per-month bias/RMSE/r + exceedances; history/*.json already has everything.
- Open question from Jun: match old AIRPACT5 basemap? (Leaflet; tile provider unidentified — compare Esri topo side-by-side first.)

## Floating legend (design A approved: bottom-center bar)
- Legend moved OUT of sidebar INTO the map (bottom-center float, 320px, dark chip styling): same #legendTitle/#legend ids so buildLegend() untouched — auto-tracks pollutant/mode/view. Collapsible (▾) to a small gradient chip, state in localStorage ('legendOpen'). Avoids MapLibre furniture (scale bottom-left, attribution bottom-right). Sidebar section removed.

## Merged header chip + hover readout (designs approved: line1=pollutant, line2=time)
- .map-clock chip REMOVED; .map-tag now two lines: line1 "<pollutant> surface forecast · PNW 4 km" (daily: "Overall AQI daily (worse of…)" / "PM2.5 24-h mean, daily" / "Ozone MDA8, daily"), line2 big time + date + FCST HR badge (daily: date + "Day n of 3" + Dn). All writes consolidated in updateClock; refresh()'s childNodes[1] map-tag hack removed.
- **Hover readout**: map-level mousemove shows value tooltip via ovTip ("6.8 µg/m³ · AQI 38 Good"; daily variants incl. Overall worse-of) — O(1) valueAt lookup; feature tooltips take precedence via queryRenderedFeatures guard (smoke/fires/class1/tribal); hidden outside domain + on mouseout. "click to query" caption removed; hint now "Hover for values · click for the 72-hour time series · drag the slider to animate". Touch devices unaffected (click flow unchanged).

## Hint relocation + time slider merged into chip (designs approved)
- Hint: below header chip (top:148px, left-aligned), auto-fades 15 s, show-once per browser (localStorage hintSeen)
- **Valid time section REMOVED from sidebar entirely**: scrub/play moved into the map chip as row 3 (same element ids — configScrub/setPlay/applyHash untouched), ticks row 4; speed = single cycling chip (0.5×/1×/2×, SPEEDS array); .map-tag width 302px, pointer-events:none REMOVED (chip now interactive — was a silent killer); daySel/secTime/clock DOM+refs deleted; "worse of" note relocated into daily-Overall legend
- Sidebar now: View, Find a place, Forecast cycle, Pollutant, opacity, Overlays
- **Hourly/Daily toggle also moved into the chip** (row 1 right side, same #viewSeg id → setView/hash/no-daily-disable untouched); sidebar View section removed; "PNW 4 km" caption dropped from chrome. Final split: chip = WHEN (view/time/playback), sidebar = WHAT/WHERE (find place, cycle, pollutant, opacity, overlays)
- Pollutant-into-chip: designed (dropdown + mini AQI/Conc toggle, no height growth) but HELD OFF at Jun's call after my pushback — dropdown hides the PM/O3 choice from casual users; sidebar seg keeps one-click discoverability + unit subtitles. Revisit only if map-first minimalism becomes the priority.

## Opacity relocated into legend header (option A approved)
- Sidebar "Layer opacity" section removed; slider (same #opacity/#opVal ids → handler untouched) now sits in the floating legend's header row between title and collapse arrow (64px mini-slider). Adjust-while-watching; hides with legend collapse (intentional). Sidebar final: Find a place, Forecast cycle, Pollutant, Overlays.

## UI consolidation arc (late 07-10) — SUMMARY
The evening became a full interface consolidation, executed as preview → approve → build, one piece at a time (Jun committing/deploying/testing each: 0f5ad41 slider, dfb72c4 view toggles, 5f93c4e opacity — all confirmed working):
1. Legend → floating bottom-center map card (collapsible, localStorage)
2. Clock chip + pollutant tag → ONE header chip (line1 layer, line2 time)
3. Hover value readout (feature tooltips take precedence); "click to query" retired
4. Onboarding hint → below chip, 15 s fade, show-once per browser
5. Time slider + play + speed → chip rows 3-4 (speed = cycling chip)
6. Hourly/Daily toggle → chip row 1
7. Opacity → legend header mini-slider
8. Pollutant selector → deliberately KEPT in sidebar (discoverability > minimalism)
END STATE: map chip = WHEN (view/time/playback/readout); legend card = layer meaning + opacity; sidebar = WHAT/WHERE (Find a place, Forecast cycle, Pollutant, Overlays). Sidebar went from 8 sections to 4.
Recurring technique that made refactors safe: MOVE elements keeping their ids — handlers/hash/config code untouched every time. Recurring trap: decorative chips had pointer-events:none (bit us when chips became interactive).

## Collapsible map chip (design approved, built)
- ▴ in chip row 1 folds the card into a one-line pill: mini play button (icon synced w/ setPlay), pollutant, live ticking time + hr/day badge (updateClock writes both full+mini); pill click expands; playMini stopPropagation so play doesn't expand. localStorage 'chipOpen'. Every floating card now folds to a live pill (chip + legend — consistent gesture).

## Pinnable comparison cards (design approved, built)
- Sparkline popups (map-click + monitor) gain "⊕ pin": converts to a draggable screen-anchored .pin-card (drag by header, clamped to map-wrap) + color-matched .pin-mark dot at origin (6-color palette cycles); × closes both. Regular popup behavior unchanged (pin hidden inside cards via noPin param).
- sparkSync single-callback → SPARK_SYNCS Set: ALL open sparklines (popups + every pinned card) follow the animation cursor together; wire() returns its show fn for external dereg.
- Pinned cards auto-close on cycle switch (closeAllPins in switchCycle) — cards would show stale-cycle data otherwise.

## Site capability markers (option A approved; chip-footer key per Jun)
- Three marker states per selected pollutant: colored circle = obs now; hollow ring = measures it, no obs this hour; small gray DIAMOND = no sensor for this pollutant (fixes ring ambiguity). Never diamonds in daily-Overall.
- Capability cached per marker (latest-obs keys ∪ any series data, set in loadObsSeriesForDots).
- Hover tooltip on dots (new): "Yakima-4th Ave · measures PM2.5 only" via map.project + ovTip.
- Chip footer key row (.tag-sites, hairline-separated, below ticks): "sites: ● obs · ○ no obs this hour · ◆ no <pol> sensor" — pollutant-aware, hidden in Overall/monitors-off/obs-unavailable; updateSiteKey() from recolorMonitors.
- BUG + fix (broke live, Jun caught): recolor did `el.className='mon-dot mon-diamond'` which WIPES MapLibre's marker classes (position:absolute) → dots fell out of map positioning; also CSS transform:rotate conflicts w/ MapLibre's inline translate. LESSONS: never overwrite className on MapLibre marker elements (classList only); never use CSS transform on them (clip-path for shapes).

## Session end state
- Everything above deployed + confirmed by Jun ("great") through commit 5f93c4e
- Queue unchanged: Phase 2 GOES-R FDC FRP (task #25); monthly statistics ~Aug 1; optional pollutant-into-chip revisit
